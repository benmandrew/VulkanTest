#ifndef __STB_H_INCLUDED__
#define __STB_H_INCLUDED__

// stb doesn't seem to have header guards,
// so this adds a wrapper with them to
// avoid multiple inclusion.

#define STB_IMAGE_IMPLEMENTATION
#include <stb_image.h>

#endif